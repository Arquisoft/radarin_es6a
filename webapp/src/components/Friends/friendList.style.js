import styled from 'styled-components';

export const List = styled.input`
    margin-top: .4em;
    float: right;
    transform: scale(1.2);
    margin-right: .6em;
    `;
export const Ul = styled.ul`
    height: auto;
    overflow-y: scroll;
`
;

