import styled from 'styled-components';

export const List = styled.div`
.lista {
    margin-top: .4em;
    float: right;
    transform: scale(1.2);
    margin-right: .6em;
}`;
export const Ul = styled.ul`
.ul-format {
    height: auto;
    overflow-y: scroll;
}`
;

